#!/bin/bash
result=""

while read line; do
	if [[ ! -z $line && $line != '#'* ]]; then
		result="$result $line"
	fi
done < /etc/chromium-flags.conf

# Last line
if [[ ! -z $line && $line != '#'* ]]; then
	result="$result $line"
fi

result=$(echo -n $result | xargs)
echo -n "$result"
